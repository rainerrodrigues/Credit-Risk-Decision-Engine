global:
  scrape_interval: 5s # Scrape data every 5 seconds

scrape_configs:
  - job_name: 'credit_risk_fastapi'
    static_configs:
      # host.docker.internal allows Prometheus (inside Docker) 
      # to talk to FastAPI running on your local machine
      - targets: ['api:8000']
